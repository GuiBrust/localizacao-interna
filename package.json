{
  "name": "app-pizzaria",
  "version": "1.0.0",
  "scripts": {
    "install-deps": "yarn global add concurrently && cd backend && yarn install && cd ../frontend && yarn install",
    "dev": "concurrently \"cd backend && yarn dev\" \"cd frontend && yarn dev\""
  }
}